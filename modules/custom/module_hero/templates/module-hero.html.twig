{# attach css #}
{{  attach_library('module_hero/hero-style') }}
<h1 class="animated bounce">{{title}}</h1>
<ol>
    {% for item in items %}
        <li class="animated {{ random(['shake', 'headShake', 'swing']) }} "><p>{{ item.name }}</p></li>
    {% endfor %}
</ol>
